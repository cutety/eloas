package com.cutety.eloas.repository;

import com.cutety.eloas.entity.UserValue;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserValueRepository extends JpaRepository<UserValue,Integer> {
}
